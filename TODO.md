# TODO.md — POEassist / Exile Assistant

Short, actionable list. Phases live in `ROADMAP.md`; history in `CHANGELOG.md`.

## Next

- (empty — nothing queued)

## Deferred
- [ ] **Game assets: re-encode the oversized files.** `divine.png` (145KB), `exalt.png` (161KB), `mirror.png` (108KB) and `chaos.png` (40KB) are full-res art being displayed at 14–16px. The registry already prefers `divine.webp` (37KB) over `divine.png`; chaos/exalt/mirror have **no webp variant**, so they're served at full size. Downscaling them to ~64px webp would cut ~300KB off the Gear Planner's first paint. Deferred because the task said not to generate/modify images — needs the user's OK to re-encode.
- [ ] **Game assets: more icon surfaces.** Currency icons are only on the Gear Planner *summary* totals + Mirror helper. Candidates: the Gear Planner table's Min/Max/Bought cells (skipped — would clutter every row and the columns are user-resizable), the Farm Planner Run Tracker's Dv/C/Ex unit dropdowns and the cost field, Divine Market's price displays, and the Exile Hub cards. Add only where the value is genuinely a currency amount.
- [ ] **Game assets: unused/uncertain files.** `logo.png` is intentionally unused (trademark). `the-divine-orb-art-...-j1j35gwrxyhg1.webp` is a duplicate Divine Orb artwork with a junk filename — not registered (`divine.webp` covers it); delete it or rename it if it's wanted. `trial of the sekhemma.webp` is mapped to Zarokh/Trial of the Sekhemas despite the filename's spelling ("sekhemma" vs the game's "Sekhemas") — verify that's the intended asset. `Atziri.webp` is mapped to "Atziri, the Red Queen".
- [ ] **Mobile card layout pass** — deliberately out of scope for the 0.31 foundation pass (desktop/web polish only). 480px currently has no horizontal overflow on any tab, but the Gear Planner table and Farm master/detail are still desktop-shaped on phones. The intended follow-up is converting the shop table rows into cards below ~700px. Do this as its own pass.
- [ ] **Command Center** — explicitly not started in 0.31. The toast/state/flash primitives (`showToast`, `uxEmptyState`, `uxSkeleton`, `uxFlash`, `uxBusy`) exist now and should be reused by it rather than re-invented.
- [ ] Toast/state adoption, remaining surfaces — Exile Hub and Divine Market still use their own `.hub-missing` / `.radar-fallback` one-off empty markup, and the Gear Planner "gear board" slot tiles have no empty-state affordance beyond the corner `+`. Low priority; migrate opportunistically when touching those tabs, not as a bulk rewrite.
- [ ] i18n: the 0.31 toast strings are Thai-only except the three that go through `t()` (`backup.export.ok`, `toast.saved`, and the reused `contact.*`/`backup.*` keys). Folded into the existing "migrate more UI strings" item below — no separate work needed, just don't hardcode new user-facing strings if that migration ever happens.
- [ ] Keyword Help Bridge: broader coverage — first pass is a **curated** `KW_HELP_TERMS` list (~31 keywords) on four surfaces (Resistance Checker, Gear Planner option, Farm card detail, Content Codex detail). Expanding = append verified ids to that array and/or call `renderKeywordHelpListFromText()` from another renderer (Market Radar reco reasons and Exile Hub are the obvious next candidates). **Never** auto-link all 766 keywords by walking the DOM — noise + it would break editable fields.
- [ ] Keyword Codex: full English descriptions second pass (0.29 added `descEn` **only as a fallback for entries with no Thai description** — storing EN for all 717 paired entries would roughly double the JSON and needs a language toggle, likely tied to `poeAssist.language.v1`, before it's worth it).
- [ ] Keyword Codex: ~47 EN-only entries (mostly `*_Rune` league keywords not yet localized on poe2db TH) show "needs review" + "EN fallback" — re-run `node scripts/update-keyword-codex.mjs` after big poe2db TH updates to pick up new translations.
- [ ] Keyword Codex: category rules (`KWC_CATEGORY_RULES` in the script) are deliberately coarse — 458 keywords match no category (visible only under "All"). Refine rules if users ask, but keep them transparent regex-on-English-name only.
- [ ] Content Codex: more parser coverage — `Irradiated` + `Map juicing` are `Stub` ("needs parser support": non-standard page layouts), and several mechanic pages have prose-only Rewards sections (captured in Notes, no structured drop rows). Extend the parse helpers in `scripts/update-content-codex.mjs` when worth it.
- [ ] Content Codex: image license audit — currently **no wiki images are embedded** (game assets © GGG, file pages carry no explicit license → placeholder + wiki link). If embedding is ever wanted, audit per-file licensing on the wiki first and store proof in the JSON (`image.licenseNote`), never hotlink blindly.
- [ ] Content Codex: optional link into Farm Planner economics — e.g. "plan a farm around this content" button that pre-fills a Farm Planner card from a codex entry. Product decision needed; keep the two tabs decoupled until then.
- [ ] Content Codex: consider a scheduled refresh in Actions (weekly, not hourly) once rate-limit behavior is understood — the wiki returned 429s even at 1 req/600ms during back-to-back runs; current stance is manual `node scripts/update-content-codex.mjs`.
- [ ] ~~Atlas Farm V.2 tab~~ **removed in patch 0.20** (nav/page/JS/CSS deleted). The orphaned key `poe2FarmLabV2.runs.v1` is kept in `APP_DATA_KEYS` only for backup continuity. If run-economics modeling is ever revisited, fold it into Farm Planner instead of a separate tab — and still only with reliable per-content reward data (no guessed rates).
- [ ] Replace placeholder `README.md` (currently just a title) with a short public-facing description + live-site link.
- [ ] Extend Settings further (theme/mode 0.13; startup tab + data overview + export/import + scoped resets 0.15; category layout + language + display currency + rate editor + font size + Contact/Donate 0.27) — remaining candidates: sidebar default state, moving the in-Shop table-sizing controls into Settings.
- [ ] i18n: migrate more UI strings into the 0.27 `I18N` dictionary (`t()`/`data-i18n`) — currently only the Settings modal + Contact/Donate are translated; all tab content, `parseBackup` errors, and PATCH_NOTES remain Thai-only. Migrate gradually per tab; never bulk-rewrite `index.html`.
- [ ] Donate: placeholder only since 0.27 ("ยังไม่เปิดให้บริการ / Not available yet") — when a payment channel is decided, add real links/QR in the Settings Donate category (product decision + probably an image asset policy check).
- [ ] Divine Market: optional opt-in persistence for user-added price points (currently session-only by design — needs a product decision + additive key before building).
- [ ] Market Radar: sortable/limited "Top Rising Items Used" list (sort by value/trend/score, show top-20 + expand) — the list gets long on big snapshots.
- [ ] ~~Atlas Planner: compact/collapsed card mode~~ — superseded by the 0.18 master/detail redesign (left list already is the compact scan view).
- [ ] Atlas Planner remaining follow-up: optional **manual** Dv↔C↔Ex conversion rates (explicit user-entered only — never hidden) so mixed-unit runs can show a combined Net/per-hour. Deferred by design in 0.19 (mixed units still show grouped totals + "per-hour conversion unavailable"). ~~persist selected card / Project runs~~, ~~per-run timestamp~~, ~~Div/hour~~ all shipped in patch 0.19.
- [ ] Gear Checker: preset naming (rename "ชุดที่ 1/2/3" to user labels) — needs a small additive storage field.
- [ ] Resistance Checker: "Add needed stat to Gear Planner" bridge from Missing Stat Radar recommendations (e.g. one-click add a shopping row like "item with +X% Cold Res"). Deferred from patch 0.23 by design — needs a product decision on row shape/slot; keep the two tabs decoupled until then.
- [ ] Resistance Checker: per-element targets (radar currently has one shared Elemental target + Chaos + Rarity — split into Fire/Cold/Lightning if ever needed; key `poe2ResForge.targets.v1` is tolerant so adding fields is additive).

## Known Issues

- [ ] Pre-Divine-Market line numbers in `PROJECT_INDEX.md`'s feature map (Exile Hub, Market Radar, Farm Planner, Gear Checker, Shopping List, Patch Notes, Cross-cutting sections) have drifted (`index.html` is now ~5600 lines). A drift warning is in place — always `rg` the anchor term first. Refresh the numbers opportunistically on the next `index.html` task.

## Done

- [x] Game asset registry (patch 0.32) — `image/`'s 13 official GGG files (currency orbs + league-mechanic icons) surfaced app-wide via one **hand-verified** table (`GAME_ASSETS`/`ASSET_ALIASES`/`RADAR_ASSET_BY_KEY`/`KWC_ASSET_IDS` + `normalizeAssetKey`/`getLocalAssetForName`/`renderAssetIcon`). Icons on Gear Planner Div totals + Mirror helper, Market Radar reco cards (6 buckets), Content Codex list + detail (8 entries), Keyword Codex (5 ids), Farm Planner list. No fuzzy matching — unmatched = no icon; broken image = hidden. **Policy:** artwork is © GGG used as fan content; `logo.png` never used (trademark); GGG attribution required in Settings → ติดต่อ; Content Codex still never embeds wiki images; `assets/icons/*.svg` stay original. Old "never copy official artwork" rule rewritten in `EDIT_GUIDE.md`. 32/32 Playwright checks. 2026-07-15.
- [x] Modern UX Foundation (patch 0.31) — three reusable systems in one new JS/CSS block: **toasts** (`showToast`/`showSuccessToast`/`showWarningToast`/`showErrorToast`/`showInfoToast`; bottom-right, aria-live, pause-on-hover, close button, `key` dedupe, z-index 400 above the modal), **unified empty/loading/error states** (`uxEmptyState`/`uxSkeleton` → applied to Gear Planner, Farm Planner, Keyword Codex, Content Codex, Market Radar — each with a real action like "ล้างคำค้น/ฟิลเตอร์" or "ลองโหลดใหม่"), and **micro-interactions** (`uxFlash` on new/copied rows+cards, `uxBusy` on Export/Import, pressed state, app-wide `:focus-visible` ring, all `prefers-reduced-motion`-guarded). 5 of 6 `alert()`s became toasts; **every `confirm()` kept**. No formula/scoring/storage change. 33/33 Playwright checks. 2026-07-15.
- [x] Keyword Help Bridge (patch 0.30) — `data/keyword-codex.json` reused app-wide: curated `?` badges / keyword chips in Resistance Checker (gauges + Missing Stat Radar + Augment Calc), Gear Planner (Important Option), Farm Planner (card detail), Content Codex (detail), all opening one shared accessible tooltip (Thai → EN fallback badge → none; poe2db links; "เปิดใน Keyword Codex" jump that selects the entry). Curated list only, nothing injected into editable fields, no new storage key. Fixed a boot-time TDZ crash for users with saved rows/cards, a scroll-closes-instantly bug, and the chip→tooltip hover gap. 2026-07-14.
- [x] Keyword Codex EN description fallback (patch 0.29) — entries with no Thai description show the English description from the poe2db US page instead (new `descEn` stored only for fallback entries + `descLang`; secondary "EN fallback" badge on list/detail/compare; both-missing = "ไม่มีคำอธิบาย / No description available"; search covers fallback text; generator strips PoE-internal `<<...>>`/brace markup from EN rune descriptions). No manual translation. 2026-07-14.
- [x] Keyword Codex tab (patch 0.28) — new Tools-group tab: poe2db TH/EN keyword encyclopedia (766 keywords, 717 paired by URL slug — names never translated by us, unpaired = "needs review" badge; Thai-only descriptions this pass). New generator `scripts/update-keyword-codex.mjs` (2 requests/run, manual only) → `data/keyword-codex.json`. UI: TH/EN/description search with safe highlight, 10 live-count filter chips (inferred category facets), master/detail, favorites (new additive key `poe2KeywordCodex.favorites.v1`, backup-covered), clickable related-keyword chips, 2–3 keyword Quick Compare (in-memory), copy buttons, poe2db TH/EN source links. 2026-07-14.

- [x] Settings upgrade (patch 0.27) — two-pane category layout (Appearance / ภาษา·สกุลเงิน / ข้อมูล·Backup / ติดต่อ / Donate / Danger Zone; horizontal tabs ≤640px); TH/EN language setting (`poeAssist.language.v1`, `I18N`/`t()` helper — Settings+Contact/Donate translated, tabs still Thai); display currency THB/USD/GBP/EUR/JPY (`poeAssist.fiatCurrency.v1` + per-currency user rates in `poeAssist.divineFiatRates.v1`; legacy THB rate read/written safely, unset rate shows "—"; Gear Planner + Market Radar fiat unit follow it, Divine Market stays THB by design); font size small/normal/large (`poeAssist.fontScale.v1`, body zoom + `uiZoom()` positioning fixes); Contact (mailto + copy) and Donate placeholder; new keys in backup + UI-prefs reset. 2026-07-13.

- [x] Resistance Checker Augment Calc `?` help tooltips (patch 0.26) — the 0.25 in-panel explanation paragraphs became compact circular `?` badges after each section title; detailed tooltip (formula, +22/+11 per Augment, 40/45%–25/20% rolls, color legend, resistance-vs-item-count distinction) on hover/keyboard focus/tap, Esc/blur/outside close, viewport-clamped fixed positioning (scroll repositions while focused). Calculations untouched. 2026-07-13.
- [x] Resistance Checker default stat template + row icons + Augment Calc explanations (patch 0.25) — Item Editor pre-fills per-stat template rows for the 5 common stats the item lacks (fixed colored label + % input; lazy materialization on first keystroke — no preset migration, no duplicates, focus preserved; ghost rows from 0.24 replaced); original inline-SVG stat icons per row (swap on select change); `.ac-sec-desc` explanation lines in both Augment Calc sections (simulated resistance vs. missing-gear-pieces meaning, +22/+11 per Augment, 40/45%–25/20% rolls, "estimate not a shopping rule"). 2026-07-13.
- [x] Resistance Checker Augment Calc + 5-row Item Editor + app-wide numpad (patch 0.24) — new `#augCalc` panel under the Item Editor (`.editor-col` wrapper in the middle `.gear-layout` column): Augment Result (F/C/L/Chaos, elemental +22 / chaos +11 per Augment, +1/+2/+3 with ready/short colors) + Items Needed Estimate (`ceil(missing/roll)`, rolls 40/45% elemental · 25/20% chaos, raw shown; no Rarity by design), fed live from the same totals/targets as the Missing Stat Radar (no new storage key). Item Editor pads to 5 rows with clickable ghost rows. On-screen numpad now opens on every numeric input app-wide (`.num-input` + `type=number` + `inputmode` numeric/decimal; pending-dot buffer makes decimals work on number inputs); Gear/Farm Planner numpad behavior unchanged. 2026-07-13.
- [x] Resistance Checker Missing Stat Radar + Readiness Score (patch 0.23) — new `#statRadar` panel right of a narrowed Item Editor: per-stat current → target → delta with ready/capped/short +X/optional statuses (same penalty-included totals as the gauges), transparent 0–100 Readiness checklist (Elemental 45 / Chaos 20 / Rarity 15 / Gear Filled 20) with breakdown, short Thai recommendations (worst element first → chaos → rarity → empty slots), and in-panel editable targets (defaults 75/0/150) in new additive key `poe2ResForge.targets.v1`. Resistance math/OCR/presets untouched. 2026-07-13.
- [x] Gear Planner Mirror-equivalent helper (patch 0.22) — small line under the planned-budget รวม Min/รวม Max Div totals: `Short of 1 Mirror` below one Mirror, `X Mirror + Y Div` (green) at/above; Mirror price from the existing `data/market-radar.json` snapshot (already includes Mirror of Kalandra via the ≥1-Div rule — no script change); helper hides when the price is unavailable (no fallback price); budget math/THB untouched. 2026-07-12.
- [x] Content Codex tab (patch 0.21) — new Tools-group tab: poe2wiki boss/content drop knowledge base (34 entries: 5 league mechanics, 19 content, 10 pinnacle bosses; Simulacrum fully parsed incl. estimated unique pool rates, Raven's Reflection 1–2x, version history). New generator `scripts/update-content-codex.mjs` (MediaWiki API, wikitext section/table parsing, per-page error tolerance, 429 backoff) → `data/content-codex.json`; manual refresh only. UI: search + 8 filters + favorites (new additive key `poe2ContentCodex.favorites.v1`), master/detail with drop table + version history right / access-mechanics-monsters left, wiki item hyperlinks, visible CC BY-NC 3.0 attribution, no images embedded. 2026-07-12.
- [x] Farm Planner time-input + nav cleanup + tab renames (patch 0.20) — (1) Duration/run is now H / M / S compact numpad fields, stored back into `durationPerRunMinutes` as fractional minutes (old values load unchanged); one display format `farmFmtHMS` (`01 H 15 M 23 S`, hours dropped when 0) used in the list, projection, and summary. (2) Left-list projection appends a projected-net `.fl-profit` chip (green/red, neutral when net not computable — mixed units never guessed). (3) Right-detail header is an equal 2-col grid (Duration | Cost, Cost still prominent). (4) Removed the in-dev **Atlas V.2** tab entirely (nav/page/JS/CSS/TAB maps; key kept for backup only). (5) Renamed visible tabs: Atlas Planner→**Farm Planner**, Shopping List→**Gear Planner**, Gear Checker→**Resistance Checker** — internal ids `farm`/`shop`/`forge` and all storage keys unchanged. 2026-07-12.
- [x] Atlas Planner Run Tracker analytics + persistence polish — `computeFarmRunAnalytics()` adds Net avg/run, Net-or-gross per-hour (compatible units only, priority Dv), Best/Worst run, win rate, cost-recovered state, and a one-line trend insight to the actual-runs summary; additive per-run `date` column (default today); selected card + Project runs now persist across reloads via new additive key `poe2FarmPlanner.view.v1` (stale/deleted selection falls back safely). No hidden currency conversion; `poe2FarmPlanner.cards.v1` shape unchanged; patch 0.19 (2026-07-12).
- [x] Atlas Farm Planner master/detail redesign — searchable compact list left / full card detail right; additive per-card fields (`durationPerRunMinutes`, `costPerRun`, `costCurrency`, `runTrackerRows`) on the unchanged `poe2FarmPlanner.cards.v1` key; per-card Run Tracker (Run # / Result 2×amount+unit / Note) + actual-runs summary; planning-only "Project runs" projection in the left list; shared numpad extended to Atlas numeric fields; patch 0.18 (2026-07-12).
- [x] Atlas Farm V.2 — new separate Run Economics tab (Planning group): entry cost / Guaranteed Floor after haircut / Net / ROI / break-even, transparent Farm V.2 Score (7 dimensions), Simulacrum + Boss/Fragment + Trial + Custom templates, market-snapshot price prefill with Manual fallback, Bigdrop Upside Notes excluded from ROI; new additive key `poe2FarmLabV2.runs.v1`; old Atlas Planner untouched; patch 0.17 (2026-07-12).
- [x] App-wide QoL/UX pass — league "Ends in" countdown (Hub + Divine), Market Radar TOP PICK badge + Enter-to-apply, Divine section reorder + form prefill, Atlas filter counts + last-checked age warnings, Gear preset data dots + res-cap counter, Shopping quick-filter counts + slot-view label; all additive, no storage keys changed; patch 0.16 (2026-07-12).
- [x] Settings app control center — startup-tab preference (new additive key `poeAssist.startTab.v1`), read-only local-data overview, Export/Import all local data (backup file), and `confirm()`-gated scoped resets (UI-only / Shopping / Atlas / Gear); patch 0.15 (2026-07-12).
- [x] Trust Blue Pay coverage polish (comprehensive rounded/soft/navy-accent overrides across all major surfaces, dark-mode grey repairs, RawBlock untouched) + original inline-SVG favicon (resolves the `/favicon.ico` 404); patch 0.14 (2026-07-12).
- [x] Theme switcher (RawBlock + Trust Blue Pay) + dark/light mode (topbar toggle + Settings, synced) via `data-theme`/`data-mode` token overrides; functional Settings; removed the redundant Shopping List standalone Type column (data model/export unchanged). New additive keys `poeAssist.theme.v1` / `poeAssist.colorMode.v1`; patch 0.13 (2026-07-12).
- [x] App layout polish: wider content (max-width 1760), collapsible sidebar (new additive key `poeAssist.sidebarCollapsed.v1`), Settings placeholder button/modal above Patch Notes, and readable Gear Plan slots (full labels + original per-slot icons, 2×2 jewels, corner quick-add); patch 0.12 (2026-07-12).
- [x] Shopping List gear-board coverage states + per-slot quick-add + Excel-like column-width/row-height sizing (new additive key `poe2ShopList.tableSizing.v1`); patch 0.11 (2026-07-12).
- [x] Shopping List equipment-slot planner — left POE2 gear board filters the table per slot, additive `slot` field (old rows → Unassigned), on-screen numpad for all Divine-price inputs; patch 0.10 (2026-07-12).
- [x] Shopping List usability pass — buy-next strip, name field (additive), quick filters, display-only sort, progress bar, clearer remaining-cost calc; patch 0.9 (2026-07-12).
- [x] Full RawBlock UI redesign of `index.html` per `design.md.md` — shipped as patch 0.8; all 6 tabs verified via headless-browser screenshots; original SVG icon set added (2026-07-12).
- [x] Committed active draft docs (`SKILL.md`, `AGENTS.md`, `design.md.md`, both workflow-skill docs); `Index.md` deliberately left untracked as a superseded draft (2026-07-12).
- [x] Reconciled `PROJECT_INDEX.md` with `EDIT_GUIDE.md`/code: added the Divine Market tab (6th tab, verified in `index.html` `TAB_IDS`), corrected line count, added line-number drift warning (2026-07-12).
- [x] Docs foundation pass: added `ROADMAP.md`, `TODO.md`, `CHANGELOG.md`, `DOC_UPDATE_RULES.md`; linked from `PROJECT_INDEX.md` (2026-07-12).
