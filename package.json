{
  "name": "exile-assistant",
  "version": "0.63.0",
  "private": true,
  "type": "module",
  "description": "Exile Assistant — personal PoE2 build/gear/trade/farming assistant (static GitHub Pages app).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:bridge": "vite build -c vite.bridge.config.ts",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
